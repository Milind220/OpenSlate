{
  "name": "@openslate/tui",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run ./src/main.ts",
    "build": "bun build ./src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@openslate/core": "workspace:*",
    "@openslate/sdk": "workspace:*",
    "@openslate/server": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/bun": "^1.2.0"
  }
}
