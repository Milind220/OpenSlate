/**
 * Provider adapter registry.
 * Each adapter normalizes a specific provider's API into the ModelAdapter interface.
 */

export type { OpenAICompatibleAdapterConfig } from "./openai-compatible.js";
