{
  "name": "@openslate/core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/bun": "^1.2.0"
  }
}